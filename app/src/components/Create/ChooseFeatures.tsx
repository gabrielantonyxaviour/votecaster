import React from "react";

export default function ChooseFeatures({
  isSybil,
  setIsSybil,
}: {
  isSybil: boolean;
  setIsSybil: (value: boolean) => void;
}) {
  return <div className="h-[65%] w-full bg-[#FBF6FF] rounded-xl"></div>;
}
